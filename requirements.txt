# Core dependencies - required for the demo and the framework.
opencv-python>=4.5
numpy>=1.21
pyyaml>=6.0

# HIL server (Flask) and HTTP client.
Flask>=2.0
requests>=2.28

# Optional: cloud / LLM decisions. Only needed if you set LLM_API_KEY.
# python-dotenv>=1.0

# Optional: live screen-capture environment.
# Enable these ONLY if you use `environment: "live"` on a system you control.
# mss>=9.0
# pyautogui>=0.9
# pygetwindow>=0.0
# keyboard>=0.13   # ESC abort hotkey for live mode

# Testing
pytest>=7.0
pytest-cov>=4.0  # test coverage measurement
