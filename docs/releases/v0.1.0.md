# Release Notes — v0.1.0

**Release date**: 2026-06-29
**Codename**: Initial public release

---

## Highlights

- **Rebranded as Vision FSM Agent** — a general-purpose computer-vision +
  finite-state-machine + human-in-the-loop framework for local, controlled
  demos, research, and education.
- **Reproducible synthetic demo** — `examples/visual_grid_world/run_demo.py`
  renders a grid world with OpenCV/numpy. No game, screen capture, or
  external software required. Runs headless and is deterministic given a
  fixed seed.
- **Modular architecture** — 6 independent core modules:
  - `src/vision_fsm_agent/fsm.py` — generic FSM engine (guards, actions, callbacks, history)
  - `src/vision_fsm_agent/vision.py` — multi-scale OpenCV template matching
  - `src/vision_fsm_agent/agent.py` — local rule-based + optional LLM-backed decision agent
  - `src/vision_fsm_agent/hil/client.py` / `server.py` — human-in-the-loop correction
  - `src/vision_fsm_agent/main.py` — environment-agnostic agent loop
- **Safety boundaries** — default environment is synthetic; live
  screen-capture mode is opt-in with PyAutoGUI failsafe enabled. No
  anti-detection or evasion features.
- **61 pytest tests** covering FSM, vision, HIL, config, agent, and
  end-to-end demo execution.
- **GitHub Actions CI** — multi-OS (Ubuntu + Windows) × multi-Python
  (3.9 / 3.11 / 3.13) matrix with coverage reporting + separate lint
  workflow (ruff + mypy + bandit).
- **Complete OSS governance** — MIT License, CONTRIBUTING,
  CODE_OF_CONDUCT, SECURITY, AGENTS, CHANGELOG, CITATION.cff, issue
  templates, PR template with safety checklist, CODEOWNERS,
  dependabot, pre-commit hooks, Makefile, py.typed, API reference,
  troubleshooting guide, dev container, mkdocs + ReadTheDocs config.

## Safety

- Intended for **local, controlled demo environments, research, and
  education**.
- **Not** intended for cheating, botting, bypassing platform rules, or
  automating third-party systems without permission.
- No anti-detection, human-mimicry, or evasion features are included.
- The optional live mode requires explicit configuration and keeps
  PyAutoGUI's failsafe enabled.

## Verification

- Demo completes synthetic objectives (collects all items, presses all
  buttons) in ~19 steps.
- `pytest tests/ -q` — 61 tests pass.
- `python scripts/oss_readiness_check.py` — passes (66 files OK, 0
  missing, 0 risky wording hits).
- High-risk wording scan: Chinese zero-tolerance (0 hits); English
  negative-context exemption (59 legitimate safety mentions exempted).

## Breaking Changes

This is the initial release. There are no prior stable releases to
break compatibility with.

## Known Limitations

- The runtime agent-decision audit log (planned for v0.2.0) is not yet
  implemented.
- FSM topologies are defined in code, not configurable via YAML (planned
  for v0.2.0).
- No web dashboard for HIL corrections (planned for v0.2.0).
- Coverage is ~64%; target is 80%+ by v0.2.0.
- Import style uses flat imports + sys.path (relative imports planned
  for v0.2.0).

## Files

- Source: 10+ modules in `src/vision_fsm_agent/` (~1555 lines) + `py.typed` marker
- Tests: 7 files in `tests/` (~650 lines, 61 tests)
- Demo: `examples/visual_grid_world/run_demo.py` (434 lines)
- Documentation: architecture, demo, HIL workflow, safety boundaries,
  troubleshooting, API reference (5 files), release notes, maintenance
  ledger (8 files)
- Assets: 3 programmatically generated template PNGs
- CI: test workflow (matrix) + lint workflow (ruff/mypy/bandit)
- Config: pyproject.toml, Makefile, .editorconfig, .pre-commit-config.yaml,
  mkdocs.yml, .readthedocs.yaml, .devcontainer/
